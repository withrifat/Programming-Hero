const first = 'abid';
const last = 'nabid';
const fullName = first + " " + last ;
const fullName2 = first.concat(" ").concat(last); //both output is same 
console.log(fullName);


// includes = for cheack some item is available or not
console.log(last.includes('b'));