// 5. Combining Arrays
// Instructions:

// Create two arrays of your choice.
// Use the concat method to combine the two arrays into a new array.
// Print both the original arrays and the combined array using console.log().

let namE = ["Rifat", "AbuHossain", "Shafiul"];
let work = ["student", "WebDev", "Model"];
let combind = namE.concat(work);
console.log(namE);
console.log(work);
console.log(combind);
